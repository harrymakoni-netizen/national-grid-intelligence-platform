listener 1883
allow_anonymous true

# Development default only. Before any real device or feeder pilot,
# replace with per-device credentials/certificates -- Section 6.3 requires
# individually-keyed devices with transport encryption, which
# allow_anonymous plaintext explicitly does not provide.
